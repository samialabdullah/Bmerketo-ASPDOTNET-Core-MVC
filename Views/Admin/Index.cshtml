<div class="admin">
    <div class="container" >
        <h1 style="text-align:center; font-style:italic;">Back-Office</h1>
        <p style="text-align:center; margin-top:3rem; background-color:lightblue; padding:10px;box-shadow: 0 10px 6px rgba(0, 0, 0, 0.2);">Here you can perform various actions to manage users, modify user roles, register products, and display a comprehensive list of user roles. </p>
        <div class="d-grid mt-5">
            <a class="btn-theme d-grid mt-2" style="margin-bottom: 1rem; text-align:center;" asp-controller="admin" asp-action="register">Add new User</a>
            <a class="btn-theme d-grid mt-2" style="margin-bottom: 1rem; text-align:center;" asp-controller="admin" asp-action="list">Users List</a>
            <a class="btn-theme d-grid mt-2" style="margin-bottom: 1rem; text-align:center;" asp-controller="admin" asp-action="changeRole">Change Role</a>
            <a class="btn-theme d-grid mt-2" style="margin-bottom: 1rem; text-align:center;" asp-controller="products" asp-action="register">Register Products</a>
        </div>
    </div>
</div>

<style>
    .admin {
        padding: 200px;
        margin-bottom: 5px;
        background: linear-gradient(to right, lightgray, white);
        height:850px;
    }

    .btn-theme {
        box-shadow: 0 10px 6px rgba(0, 0, 0, 0.2);
    }
</style>